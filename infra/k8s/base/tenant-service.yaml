apiVersion: apps/v1
kind: Deployment
metadata:
  name: tenant-service
  namespace: cosmos
  labels: { app: tenant-service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: tenant-service }
  template:
    metadata:
      labels: { app: tenant-service }
    spec:
      containers:
        - name: tenant-service
          image: REPLACE_REGISTRY/cosmos-tenant-service:latest
          ports:
            - containerPort: 3002
          envFrom:
            - configMapRef: { name: cosmos-config }
            - secretRef: { name: cosmos-secrets }
          env:
            - name: PORT
              value: "3002"
            - name: SERVICE_NAME
              value: "tenant-service"
            - name: COSMOS_OTEL_ENABLED
              value: "false"
          readinessProbe:
            httpGet: { path: /api/v1/health, port: 3002 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /api/v1/health, port: 3002 }
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits:   { cpu: 500m, memory: 512Mi }
---
apiVersion: v1
kind: Service
metadata:
  name: tenant-service
  namespace: cosmos
spec:
  selector: { app: tenant-service }
  ports:
    - port: 3002
      targetPort: 3002
      name: http
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: tenant-service
  namespace: cosmos
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: tenant-service
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 70 }
