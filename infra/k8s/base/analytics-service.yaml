apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics-service
  namespace: cosmos
  labels: { app: analytics-service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: analytics-service }
  template:
    metadata:
      labels: { app: analytics-service }
    spec:
      containers:
        - name: analytics-service
          image: REPLACE_REGISTRY/cosmos-analytics-service:latest
          ports:
            - containerPort: 3014
          envFrom:
            - configMapRef: { name: cosmos-config }
            - secretRef: { name: cosmos-secrets }
          env:
            - name: PORT
              value: "3014"
            - name: SERVICE_NAME
              value: "analytics-service"
            - name: COSMOS_OTEL_ENABLED
              value: "false"
          readinessProbe:
            httpGet: { path: /api/v1/health, port: 3014 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /api/v1/health, port: 3014 }
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits:   { cpu: 500m, memory: 512Mi }
---
apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: cosmos
spec:
  selector: { app: analytics-service }
  ports:
    - port: 3014
      targetPort: 3014
      name: http
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: analytics-service
  namespace: cosmos
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: analytics-service
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 70 }
