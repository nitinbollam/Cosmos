apiVersion: apps/v1
kind: Deployment
metadata:
  name: storefront-service
  namespace: cosmos
  labels: { app: storefront-service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: storefront-service }
  template:
    metadata:
      labels: { app: storefront-service }
    spec:
      containers:
        - name: storefront-service
          image: REPLACE_REGISTRY/cosmos-storefront-service:latest
          ports:
            - containerPort: 3008
          envFrom:
            - configMapRef: { name: cosmos-config }
            - secretRef: { name: cosmos-secrets }
          env:
            - name: PORT
              value: "3008"
            - name: SERVICE_NAME
              value: "storefront-service"
            - name: COSMOS_OTEL_ENABLED
              value: "false"
          readinessProbe:
            httpGet: { path: /api/v1/health, port: 3008 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /api/v1/health, port: 3008 }
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits:   { cpu: 500m, memory: 512Mi }
---
apiVersion: v1
kind: Service
metadata:
  name: storefront-service
  namespace: cosmos
spec:
  selector: { app: storefront-service }
  ports:
    - port: 3008
      targetPort: 3008
      name: http
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: storefront-service
  namespace: cosmos
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: storefront-service
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 70 }
