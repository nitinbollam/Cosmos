# Next.js-only stack (@cosmos/web). Legacy per-service manifests remain in this folder but are not applied.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: cosmos
resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml
  - web.yaml
