apiVersion: apps/v1
kind: Deployment
metadata:
  name: cosmos-web
  labels:
    app: cosmos-web
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cosmos-web
  template:
    metadata:
      labels:
        app: cosmos-web
    spec:
      containers:
        - name: web
          image: cosmos/web:latest
          ports:
            - containerPort: 4000
          envFrom:
            - configMapRef:
                name: cosmos-config
            - secretRef:
                name: cosmos-secrets
          env:
            - name: PORT
              value: "4000"
            - name: HOSTNAME
              value: "0.0.0.0"
            - name: NEXT_PUBLIC_GATEWAY_URL
              value: "/api/v1"
          readinessProbe:
            httpGet:
              path: /api/v1/health
              port: 4000
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/v1/health
              port: 4000
            initialDelaySeconds: 30
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: cosmos-web
spec:
  selector:
    app: cosmos-web
  ports:
    - port: 80
      targetPort: 4000
