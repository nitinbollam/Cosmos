apiVersion: apps/v1
kind: Deployment
metadata:
  name: purchasing-service
  namespace: cosmos
  labels: { app: purchasing-service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: purchasing-service }
  template:
    metadata:
      labels: { app: purchasing-service }
    spec:
      containers:
        - name: purchasing-service
          image: REPLACE_REGISTRY/cosmos-purchasing-service:latest
          ports:
            - containerPort: 3006
          envFrom:
            - configMapRef: { name: cosmos-config }
            - secretRef: { name: cosmos-secrets }
          env:
            - name: PORT
              value: "3006"
            - name: SERVICE_NAME
              value: "purchasing-service"
            - name: COSMOS_OTEL_ENABLED
              value: "false"
          readinessProbe:
            httpGet: { path: /api/v1/health, port: 3006 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /api/v1/health, port: 3006 }
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits:   { cpu: 500m, memory: 512Mi }
---
apiVersion: v1
kind: Service
metadata:
  name: purchasing-service
  namespace: cosmos
spec:
  selector: { app: purchasing-service }
  ports:
    - port: 3006
      targetPort: 3006
      name: http
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: purchasing-service
  namespace: cosmos
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: purchasing-service
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 70 }
