apiVersion: apps/v1
kind: Deployment
metadata:
  name: compliance-service
  namespace: cosmos
  labels: { app: compliance-service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: compliance-service }
  template:
    metadata:
      labels: { app: compliance-service }
    spec:
      containers:
        - name: compliance-service
          image: REPLACE_REGISTRY/cosmos-compliance-service:latest
          ports:
            - containerPort: 3007
          envFrom:
            - configMapRef: { name: cosmos-config }
            - secretRef: { name: cosmos-secrets }
          env:
            - name: PORT
              value: "3007"
            - name: SERVICE_NAME
              value: "compliance-service"
            - name: COSMOS_OTEL_ENABLED
              value: "false"
          readinessProbe:
            httpGet: { path: /api/v1/health, port: 3007 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /api/v1/health, port: 3007 }
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits:   { cpu: 500m, memory: 512Mi }
---
apiVersion: v1
kind: Service
metadata:
  name: compliance-service
  namespace: cosmos
spec:
  selector: { app: compliance-service }
  ports:
    - port: 3007
      targetPort: 3007
      name: http
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: compliance-service
  namespace: cosmos
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: compliance-service
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 70 }
