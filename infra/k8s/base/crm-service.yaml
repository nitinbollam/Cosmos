apiVersion: apps/v1
kind: Deployment
metadata:
  name: crm-service
  namespace: cosmos
  labels: { app: crm-service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: crm-service }
  template:
    metadata:
      labels: { app: crm-service }
    spec:
      containers:
        - name: crm-service
          image: REPLACE_REGISTRY/cosmos-crm-service:latest
          ports:
            - containerPort: 3010
          envFrom:
            - configMapRef: { name: cosmos-config }
            - secretRef: { name: cosmos-secrets }
          env:
            - name: PORT
              value: "3010"
            - name: SERVICE_NAME
              value: "crm-service"
            - name: COSMOS_OTEL_ENABLED
              value: "false"
          readinessProbe:
            httpGet: { path: /api/v1/health, port: 3010 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /api/v1/health, port: 3010 }
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits:   { cpu: 500m, memory: 512Mi }
---
apiVersion: v1
kind: Service
metadata:
  name: crm-service
  namespace: cosmos
spec:
  selector: { app: crm-service }
  ports:
    - port: 3010
      targetPort: 3010
      name: http
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: crm-service
  namespace: cosmos
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: crm-service
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 70 }
