apiVersion: apps/v1
kind: Deployment
metadata:
  name: pos-service
  namespace: cosmos
  labels: { app: pos-service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: pos-service }
  template:
    metadata:
      labels: { app: pos-service }
    spec:
      containers:
        - name: pos-service
          image: REPLACE_REGISTRY/cosmos-pos-service:latest
          ports:
            - containerPort: 3009
          envFrom:
            - configMapRef: { name: cosmos-config }
            - secretRef: { name: cosmos-secrets }
          env:
            - name: PORT
              value: "3009"
            - name: SERVICE_NAME
              value: "pos-service"
            - name: COSMOS_OTEL_ENABLED
              value: "false"
          readinessProbe:
            httpGet: { path: /api/v1/health, port: 3009 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /api/v1/health, port: 3009 }
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits:   { cpu: 500m, memory: 512Mi }
---
apiVersion: v1
kind: Service
metadata:
  name: pos-service
  namespace: cosmos
spec:
  selector: { app: pos-service }
  ports:
    - port: 3009
      targetPort: 3009
      name: http
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: pos-service
  namespace: cosmos
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: pos-service
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 70 }
