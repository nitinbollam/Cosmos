# Secrets in production should come from AWS Secrets Manager via External Secrets Operator.
# This is a placeholder showing required keys.
apiVersion: v1
kind: Secret
metadata:
  name: pleros-secrets
  namespace: pleros
type: Opaque
stringData:
  DATABASE_URL: "REPLACE_AT_DEPLOY_TIME"
  REDIS_URL: "REPLACE_AT_DEPLOY_TIME"
  JWT_SECRET: "REPLACE_AT_DEPLOY_TIME"
  JWT_REFRESH_SECRET: "REPLACE_AT_DEPLOY_TIME"
  STRIPE_SECRET_KEY: "REPLACE_AT_DEPLOY_TIME"
  STRIPE_WEBHOOK_SECRET: "REPLACE_AT_DEPLOY_TIME"
