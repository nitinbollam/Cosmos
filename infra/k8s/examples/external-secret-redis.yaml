# Example ExternalSecret (External Secrets Operator): shared Redis URL for BullMQ / EventBusClient.
# Mount **`pleros-redis-url`** as env **`REDIS_URL`** on services that publish or subscribe (e.g. order-service,
# notification-service, wms-service). Adjust **`remoteRef`** to your AWS Secrets Manager (or other backend) path.
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: pleros-redis
  namespace: pleros
spec:
  refreshInterval: 15m
  secretStoreRef:
    name: cluster-secret-store
    kind: ClusterSecretStore
  target:
    name: pleros-redis-url
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        REDIS_URL: "{{ .url }}"
  data:
    - secretKey: url
      remoteRef:
        key: pleros/shared
        property: redis_url
