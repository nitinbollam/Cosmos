apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: cosmos
resources:
  - ../../base
patches:
  - target: { kind: Deployment, name: auth-service }
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 3
commonLabels:
  environment: production
