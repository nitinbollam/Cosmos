apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: pleros
resources:
  - ../../base
patches:
  - target: { kind: Deployment, name: pleros-web }
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 1
commonLabels:
  environment: staging
