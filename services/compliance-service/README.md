# compliance-service

MSA MULTICAT generation, tax liability tracking, batch expiry monitoring.

Default port: **3007**.

The MSA cron runs every Sunday at 23:00 UTC. For HA, run only one replica or implement leader-election (see MISSING.md).
